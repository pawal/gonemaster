// Package clitest runs a gonemaster CLI's entry point against buffers and
// asserts on the exit code and the two streams it wrote, so each test states
// the invocation and the expectation rather than the plumbing between them.
package clitest

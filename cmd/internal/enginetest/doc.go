// Package enginetest stubs the runEngine seam the gonemaster and
// gonemaster-nagios binaries expose, so a CLI test decides what a run reports
// without reaching the network and without restoring the seam by hand.
package enginetest

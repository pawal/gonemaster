# Baseline allowlist for legacy packed-list-only log arg keys.
# The coherency guardrail fails if a new packed-list-only key appears.

mailtarget_list
names
ns_ip_list
ns_list
nsname_list
parent_addresses
servers
zone_addresses

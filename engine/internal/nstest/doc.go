// Package nstest provides shared nameserver and recursor scaffolding for engine
// package tests. It imports nameserver and recursor, so the tests of those two
// packages cannot use it; they keep their own local helpers.
package nstest

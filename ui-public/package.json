{
  "name": "gonemaster-ui-public",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node -e \"require('fs').writeFileSync('../server/public/dist/placeholder.txt', 'This placeholder keeps the dist directory embeddable when built UI assets are not present.\\nRun make ui-build before building the default server binary to embed the public web app.\\n')\"",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "svelte": "^5.53.2"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@testing-library/jest-dom": "^6.6.2",
    "@testing-library/svelte": "^5.2.3",
    "jsdom": "^24.1.3",
    "vite": "^8.0.8",
    "vitest": "^4.1.4"
  },
  "allowScripts": {
    "fsevents@2.3.3": true
  }
}
